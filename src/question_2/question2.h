//bool test_config(); In the header file, create a vector of double value return function get_cookie_ingredients that accepts an int cookies to bake.

//bool test_config();
#include <vector>
using std:: vector;

vector<double> get_cookie_ingredients(int cookies_to_bake);

